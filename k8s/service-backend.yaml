apiVersion: v1
kind: Service
metadata:
  name: pharmacy-app-backend
spec:
  type: NodePort
  selector:
    app: pharmacy-app-backend
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30008